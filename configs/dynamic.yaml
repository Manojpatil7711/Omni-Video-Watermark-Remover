mode: dynamic
engine: ai
detector:
  backend: sam2
  labels: [watermark, logo, text, badge, timestamp]
tracker:
  backend: bytetrack
inpainting:
  backend: propainter
  batch_size: 2
  temporal_radius: 10
